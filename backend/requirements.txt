fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27              # used by scripts/check_live_connection.py (raw GraphQL preflight, independent of the SDK below)
acryl-datahub>=0.15      # official DataHub SDK -- used by LiveDataHubClient (DATAHUB_MODE=live)
# graphql-core>=3.2        # optional: enables execute_graphql's strip_unsupported_fields
#                          # cross-version safety net (used by get_lineage). Works fine without
#                          # it too -- just skips that extra defensive step and logs a notice.
# Install only the AI SDK matching the AI_PROVIDER you use:
# anthropic>=0.34
# openai>=1.40
# google-genai>=1.0        # NOT google-generativeai -- that package is deprecated by Google
